{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window: core events and app APIs.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
